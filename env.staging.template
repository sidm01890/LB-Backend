# Backend Staging Environment Configuration Template
# Copy this file to .env.staging and update the values

ENVIRONMENT=staging
DEBUG=false
HOST=0.0.0.0
PORT=8034
# Note: External port is 8035, internal container port is 8034

# JWT Configuration - CHANGE THESE SECRETS!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRES_IN=24h

# Encryption Keys - CHANGE THESE SECRETS!
SECRET_KEY=your-secret-encryption-key
IV=your-initialization-vector

# Database - SSO (AWS RDS)
SSO_DB_HOST=coreco-mysql.cpzxmgfkrh6g.ap-south-1.rds.amazonaws.com
SSO_DB_USER=admin
SSO_DB_PASSWORD=One4the$#
SSO_DB_NAME=devyani_sso
SSO_DB_PORT=3306

# Database - Main (AWS RDS)
MAIN_DB_HOST=coreco-mysql.cpzxmgfkrh6g.ap-south-1.rds.amazonaws.com
MAIN_DB_USER=admin
MAIN_DB_PASSWORD=One4the$#
MAIN_DB_NAME=devyani
MAIN_DB_PORT=3306

# Database Pool Settings
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# Organization & Tool IDs
ORGANIZATION_ID=1
TOOL_ID=1

# Email Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Redis Configuration (Docker service name)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Uploader API (Docker service name)
UPLOADER_API_URL=http://uploader:8010

# CORS - Allow staging domain and server IP
# Include both the domain name and IP addresses for staging
CORS_ORIGINS=https://reconciistage.corepeelers.com,http://reconciistage.corepeelers.com,http://65.0.236.144,https://65.0.236.144

# Task Executor
TASK_EXECUTOR_WORKERS=10

# Formula Watcher Configuration
# Interval in seconds for checking formula changes (default: 10 seconds)
FORMULA_WATCH_INTERVAL_SECONDS=3000

# MongoDB Configuration (Staging - Use host's private IP for Docker)
# In Docker, use the host machine's private IP instead of localhost
MONGO_HOST=172.31.13.243
MONGO_PORT=27017
MONGO_DATABASE=devyani_mongo
# MongoDB authentication
MONGO_USERNAME=devyani-application-client
MONGO_PASSWORD=CPPL1234
MONGO_AUTH_SOURCE=devyani_mongo

